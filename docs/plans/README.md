# Feature plans

One file per multi-session feature: `docs/plans/<feature>.md`.

Format: goal, decisions made, phased checklist with checkboxes. Claude marks phases complete as they land (commit per phase). To resume in a new session: "Read docs/plans/<feature>.md — phase N is done, start phase N+1."

Delete the file (or move the summary into a memory note) once the feature ships.
